<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>test </title>
    <style>
      *{
        margin:0;
        padding: 0;
        box-sizing: border-box;
      }
      .border{
        height: 30px;
        width: fit-content;
        border:1px solid #000;
        /* padding:10px;
        display: block; */
        justify-content: space-between;
        margin-left:1rem;
      }
      .test{
        display: flex;
        /* justify-content: space-evenly; */
        /* margin-left: 20px; */
        align-items: center;
      }
    </style>
</head>
<body>
  <h3 style="text-align: center;"><%= seances.emploiName  %> </h3>
    <table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col" style="text-align: center;">seances</th>

    </tr>
  </thead>
  <tbody>

   <tr>
      <td>lundi</td>
        <td class="test"> 
          
              <% seances.lundi.forEach((s) => {   %>
                <div class="border">
                  <%= s.startTime %>
                  <%= s.finishTime %>
                  <%= s.salle %>
                  <%= s.matiere %>
                </div> 
              <% }) %>
      </td>
   </tr>

    <tr>
       <td>mardi</td>
        <td class="test">
            <% seances.mardi.forEach((s) => {  %>
              <div class="border">
                <%= s.startTime %>
                <%= s.finishTime %>
                <%= s.salle %>
                <%= s.matiere %>
              </div> 
            <% }) %>
        </td>
   </tr>

    <tr>
       <td>mercredi</td>
        <td>
            <% seances.mercredi.forEach((s) => {  %>
              <div class="border">
                <%= s.startTime %>
                <%= s.finishTime %>
                <%= s.salle %>
                <%= s.matiere %>
              </div> 
            <% }) %>
        </td>
   </tr>

    <tr>
       <td>jeudi</td>
        <td>
            <% seances.jeudi.forEach((s) => {  %>
              <div class="border">
                <%= s.startTime %>
                <%= s.finishTime %>
                <%= s.salle %>
                <%= s.matiere %>
              </div> 
            <% }) %>
        </td>
   </tr>

    <tr>
       <td>vendredi</td>
        <td style="display: flex;">
            <% seances.vendredi.forEach((s) => {  %>
              <div class="border">
                <%= s.startTime %>
                <%= s.finishTime %>
                <%= s.salle %>
                <%= s.matiere %>
              </div> 
            <% }) %>
        </td>
   </tr>

    <tr>
       <td>samedi</td>
        <td>
            <% seances.samedi.forEach((s) => {  %>
              <div class="border">
                <%= s.startTime %>
                <%= s.finishTime %>
                <%= s.salle %>
                <%= s.matiere %>
              </div> 
            <% }) %>
        </td>
   </tr>
 
           

     

     

            

  </tbody>


</table>
</body>
</html>